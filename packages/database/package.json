{
  "name": "@jabba01/lfcr-database",
  "version": "0.1.0",
  "description": "MongoDB database connector for the LI.FI FeeCollection Reporter offchain services.",
  "author": {
    "name": "Jabba ø1",
    "email": "jabba@srenault.com",
    "url": "https://github.com/ja88a"
  },
  "repository": {
    "url": "https://github.com/ja88a/onchain-fee-collection-reporting",
    "type": "git"
  },
  "keywords": [],
  "license": "AGPL-3.0-only",
  "main": "dist/index",
  "files": [
    "/dist"
  ],
  "scripts": {
    "clean": "shx rm -rf dist/* && rm -rf coverage/*",
    "reset": "shx rm -rf dist node_modules .turbo coverage",
    "build": "tsc",
    "build:bundle": "pnpm clean && esbuild --bundle --outfile=./dist/index.js --platform=node --target=node20 --minify --sourcemap ./src/index.ts",
    "watch": "tsc -w",
    "format": "prettier --write \"src/**/*.ts\"",
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "test:cov": "vitest --coverage"
  },
  "dependencies": {
    "@jabba01/lfcr-common": "workspace:*",
    "@lifi/types": "^17.17.0",
    "@typegoose/typegoose": "^12.16.0",
    "ethers": "^5.8.0",
    "mongoose": "^8.15.1"
  },
  "devDependencies": {
    "@repo/eslint-config": "workspace:*",
    "@repo/lib-config-typescript": "workspace:*"
  }
}
